{% for part in work.parts %}
  <h3 id="{{ part.id }}">{{ part.title.en }} / {{ part.title.la }}</h3>
  <ul>
    {% for section in part.sections %}
      <li id="{{ section.id }}">
        {{ section.section_number }}. {{ section.title.en }} / {{ section.title.la }}
        {% if section.links %}
          (<a href="{{ section.links[0].url }}" target="_blank">{{ section.links[0].site }}</a>)
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endfor %}
